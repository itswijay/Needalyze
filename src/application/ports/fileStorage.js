/**
 * Port: somewhere to put generated files.
 *
 * @typedef {Object} StoredFile
 * @property {string} url
 * @property {string} path
 *
 * @typedef {Object} FileStorage
 * @property {(input: { path: string, body: Blob | ArrayBuffer | Buffer, contentType: string }) => Promise<StoredFile>} upload
 */

export {}
